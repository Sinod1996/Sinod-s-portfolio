import React from 'react';
import './Navigation.css'

function Navigation(props) {
    return (
       <nav className={'navigation'}>
           <a href="#!" className={'logo '}>Sin</a>
       </nav>
    );
}

export default Navigation;